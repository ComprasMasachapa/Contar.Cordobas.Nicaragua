<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CONTADOR DE BILLETES - MINSA</title>

<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

<style>
    body {
        font-family: 'Roboto', sans-serif;
        background: linear-gradient(120deg, #e0f7fa, #c8fff4);
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
    }

    .inicio-container {
        background: white;
        padding: 40px;
        border-radius: 20px;
        text-align: center;
        box-shadow: 0 0 20px rgba(0,0,0,0.15);
        display: none;
    }

    .btn-inicio {
        padding: 15px 40px;
        font-size: 1.2em;
        font-weight: 700;
        border: none;
        color: white;
        background-color: #00796b;
        border-radius: 10px;
        cursor: pointer;
    }

    .btn-inicio:hover {
        background-color: #004d40;
    }

    .container {
        background: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 0 20px rgba(0,0,0,0.15);
        width: 95%;
        max-width: 600px;
        display: none;
    }

    h1 {
        text-align: center;
        color: #00796b;
        margin-bottom: 25px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 25px;
    }

    th, td {
        padding: 12px;
        border-bottom: 1px solid #ccc;
        text-align: center;
    }

    th {
        background-color: #00796b;
        color: white;
    }

    input {
        width: 80px;
        padding: 8px;
        font-size: 1em;
        text-align: center;
        border-radius: 8px;
        border: 1px solid #999;
    }

    .total {
        font-size: 1.4em;
        font-weight: 700;
        color: #004d40;
        text-align: center;
        padding-top: 15px;
    }
</style>
</head>

<body>

<div class="inicio-container" id="inicio">
    <h1>CONTADOR DE BILLETES - MINSA</h1>
    <button class="btn-inicio" onclick="iniciar()">EMPEZAR A CALCULAR</button>
</div>

<div class="container" id="calculadora">
    <h1>CONTADOR DE BILLETES</h1>

    <table>
        <thead>
            <tr>
                <th>DENOMINACIÓN</th>
                <th>CANTIDAD</th>
                <th>SUBTOTAL</th>
            </tr>
        </thead>
        <tbody id="tabla">
            <!-- Filas generadas por JS -->
        </tbody>
    </table>
document.addEventListener('keydown', e => { if (e.key === 'Enter') iniciar(); });

    <div class="total">
        TOTAL: <span id="totalGeneral">C$ 0.00</span>
    </div>
</div>

<script>
const billetes = [1000, 500, 200, 100, 50, 20, 10,5];

window.onload = () => {
    document.getElementById("inicio").style.display = "block";
};

function iniciar() {
    document.getElementById("inicio").style.display = "none";
    document.getElementById("calculadora").style.display = "block";
    generarTabla();
}

function generarTabla() {
    let tbody = document.getElementById("tabla");
    tbody.innerHTML = "";

    billetes.forEach(den => {
        let fila = document.createElement("tr");

        fila.innerHTML = `
            <td>C$ ${den}</td>
            <td><input type="number" min="0" value="0" data-den="${den}" oninput="calcular()" /></td>
            <td id="sub-${den}">C$ 0.00</td>
        `;

        tbody.appendChild(fila);
    });
}

function calcular() {
    let total = 0;

    document.querySelectorAll("input[data-den]").forEach(input => {
        const den = parseInt(input.getAttribute("data-den"));
        const cant = parseInt(input.value) || 0;
        const subtotal = den * cant;
        total += subtotal;

        document.getElementById("sub-" + den).innerText = "C$ " + subtotal.toFixed(2);
    });

    document.getElementById("totalGeneral").innerText = "C$ " + total.toFixed(2);
}
</script>

</body>
</html>
